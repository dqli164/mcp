[project]
name = "weather"
version = "0.1.0"
description = "MCP Server for weather queries by city name"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.6.0",
    "httpx>=0.27.0",
]

[project.scripts]
weather = "weather:main"
